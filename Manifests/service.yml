apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    name: user
spec:
  type: LoadBalancer
  ports:
    - port: 9090
      protocol: TCP
      targetPort: 80
  selector:
    app: orderdetail-pod
